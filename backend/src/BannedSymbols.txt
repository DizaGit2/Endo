P:System.DateTime.UtcNow;Use the injected TimeProvider (plan §2 determinism)
P:System.DateTime.Now;Use the injected TimeProvider (plan §2 determinism)
P:System.DateTime.Today;Use the injected TimeProvider (plan §2 determinism)
P:System.DateTimeOffset.UtcNow;Use the injected TimeProvider (plan §2 determinism)
P:System.DateTimeOffset.Now;Use the injected TimeProvider (plan §2 determinism)
