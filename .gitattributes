# Shell scripts must reach the Linux CI runner and containers with LF endings, regardless of the
# checkout machine's core.autocrlf (P3c-T3 — deploy/vault/init.sh is bind-mounted into a container).
*.sh text eol=lf
